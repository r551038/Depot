Terraform-AWS contains a demostration of creating resources with specific parameters;
without requiring modification to IAC template.

UnixPAMPOC contains system that provides users the ability to control Unix services (Solaris v9)
without the need of elevated privileges.

WintelPAMPOC contains a privileged access management tool for just in time access to System Administrators.

WintelDiags contains utilities for a current performance report of a server and a hardware report that
deletes duplicate hardware but will show duplicates devices that are having an issue.

These tools are provided without any express guarantee of suitability.
